# Core scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
playwright>=1.40.0

# Parcel data
dbfread>=2.0.7

# PDF parsing — text-layer (primary)
pdfplumber>=0.10.0
pdfminer.six>=20221105

# PDF parsing — OCR fallback (for scanned/image PDFs)
# PyMuPDF is the fastest renderer; pytesseract uses Tesseract OCR engine
PyMuPDF>=1.23.0
pytesseract>=0.3.10
Pillow>=10.0.0
# pdf2image>=1.16.0   # optional second OCR renderer (needs poppler)

# Excel export
openpyxl>=3.1.0

# Address parsing (optional, used for GHL name splitting)
# usaddress>=0.5.10
